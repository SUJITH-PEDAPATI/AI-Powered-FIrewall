[project]
name = "ai-firewall"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SUJITH PEDAPATI", email = "pedapatisujith@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "scapy>=2.7.0",
]

[project.scripts]
ai-firewall = "ai_firewall:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
